[package]
name = "cl-noise"
description = "Deterministic hashes, value noise, fBm, mulberry32 and JavaScript number semantics, ported bit-for-bit from the prototype"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
libm = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
