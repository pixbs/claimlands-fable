Closes #

## Summary
<!-- What changed and why, in the words a reviewer needs. Reference rule ids (R-ECO-03) or prototype line ranges where they apply. -->

## How verified
<!-- Commands you ran (cargo xtask check at minimum) and what you looked at on the preview URL the bot posts below. -->

## Visual change?
<!-- "no", or "yes" with the label `visual-change` and the fixture/baseline files that changed. -->

## Checklist
- [ ] Tests cover the change (unit, property, snapshot or fixture)
- [ ] Crate README and docs updated if the public API or a rule changed
- [ ] No `TODO` without an issue reference; no new dependency without the `deps` label
- [ ] Title is `type(scope): summary`; body starts with `Closes #<issue>`
